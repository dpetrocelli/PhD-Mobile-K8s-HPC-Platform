apiVersion: apps/v1
kind: Deployment
metadata:
  name: dev-workerx86-internet
  labels:
    tier: backend
spec:
  selector:
    matchLabels:
      app: dev-workerx86-internet
      tier: backend
  replicas: 2  
  template:
    metadata:
      labels:
        app: dev-workerx86-internet
        tier: backend
    spec:
      containers:
      - name: dev-workerx86-internet
        image: dpetrocelli/dev-workerx86-internet
        resources:
          requests:
            cpu: 200m
            memory: 200Mi
          limits:
            cpu: 2000m
            memory: 2000Mi